// Optional large English word list (~100k). Replace with your full list for maximum variety.
// Expected format:
//   window.WORDS_EN_100K = ["aback","abase",...]; // lowercase words only
// If not provided, the app will fall back to words_en_5k.js.
(function(){
  // Minimal stub so the file loads successfully; replace with your 100k list.
  window.WORDS_EN_100K = [];
})();
